server {
    listen 80 default;

    server_name _;

    access_log /var/log/test.access;
    error_log /var/log/test.error;

    location ^~ /uploads/ {
	root /home/box/web;
    }

    location ~* \. {
	root /home/box/web/public;
    }
}
